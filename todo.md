AI Chat should return errors, even if the key is valid but not active, or wahtever comes out, we need it

Git sync doesn't always refresh/look correct

I really do want the projects to be stored in the tasks by their project ID. Same with linked/referenced tasks. Everything should statically be linked by ids not labels, because things may change. And I don't want to go back and rewrite it everywhere. Think pivot tables, or joins.